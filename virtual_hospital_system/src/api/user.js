import request from '@/utils/request'

  // 调用登录接口
/*export const userloginService = ( username , password ) => {
  const { username, password } = { username , password }
  return request.post('http://106.54.206.14:8080/users/login', { username, password })
}*/